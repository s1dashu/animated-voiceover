# LibTV 模型名称映射

以下映射仅适用于 LibTV CLI，其他平台不得沿用：

| 常用名称 | LibTV `modelName` |
| --- | --- |
| GPT-Image-2 | `Lib Image` |
| Nano Banana Pro | `General image Pro` |
| Midjourney | `悠船` |

具体版本、可用模型和 schema 以 `libtv model search` 与实时查询结果为准。
